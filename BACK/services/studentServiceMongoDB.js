// TODO 1: Import the User model from ../models/userModel.js

// TODO 2: Export the following functions using the correct Mongoose methods:
//   - findAllStudents()          → returns all users        (hint: .find({}))
//   - findStudentById(id)        → returns one user by id   (hint: .findById())
//   - createStudentService(data) → creates a new user       (hint: .create())
//   - deleteStudentService(id)   → deletes a user by id     (hint: .findByIdAndDelete())
//   - updateStudentService(id, data) → updates a user by id (hint: .findByIdAndUpdate())

// Note: these functions return Promises — the controller will await them
